id = "demo"
app = "demo"

[packages]
present = ["curl"]

[dotfiles]
"~/.demorc" = { source = "home/.demorc", mode = "link" }

[hooks]
pre = ["echo pre-hook >> /tmp/hooks.log"]
post = ["echo post-hook >> /tmp/hooks.log"]
