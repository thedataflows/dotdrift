# Directory Update Log

## 2026-08-03

* **Added (plan `--deps` / `--deps-depth N`)**: `dotdrift plan --deps` renders a dependency tree for every package in `packages.install`, in both text and `--json` output; `--deps-depth N` (default 1, requires `--deps`, must be >= 1) recurses deeper. New `Backend.DirectDeps(ctx, pkg)` with three implementations in `internal/packages/deps.go`: `paru -Si` (covers AUR, version specs stripped), `apt-cache depends` (PreDepends kept, `|Depends:` alternatives and `<virtual>` targets skipped), `dnf repoquery --requires --resolve --qf '%{name}\n'` (self dropped; argv unverified against a live dnf). `DepsTree` is a memoized DFS — one query per unique package, a path set terminates real pacman cycles (harfbuzz ↔ freetype2), and a failed query marks the node `Unknown` (`- pkg (deps unknown)` in text, `"unknown": true` in JSON) with a zerolog warn-and-proceed so a listing never fails on one package. Two flags rather than one optional-value flag because kong would eat the positional `modules` arg in `--deps 2` space form. Text rendering keeps the `  - pkg` lines byte-identical when `--deps` is off; JSON omits the `packages.deps` key entirely without `--deps`. Deps of the `remove` list are intentionally not shown. Task doc: `docs/tasks/t-plan-deps.md`.

## 2026-08-02

* **Changed (modules output)**: `dotdrift modules` drops the redundant second column — it printed `selected <id> <app>`, but `app` defaults to the id (`discover.go`), so nearly every line repeated itself. Selected lines are now `selected <id>` with tags only when informative: `[system]` for system scope (matching the plan output convention) and `(app: <app>)` when an explicit `app` differs from the id. Skipped lines are unchanged (`skipped  <id> <reason>` — the reason column earns its place). New fixture `testdata/profiles/appname`; tests updated in `cmd/modules_test.go` (`TestCLI_modules_appAndScopeTags`).
* **Fixed (CI root-environment failures, found via act)**: three tests passed for non-root developers but failed in root containers (act/docker/GitHub e2e): (1) `TestApply_smbStepConditional` asserted a `sudo -E` prefix that `privArgv` correctly drops at euid 0 — assertion relaxed to the prefix-free convention the mounts assertions already use (privilege logic stays hermetically covered in internal/smb via `stubSeams`); (2) `TestDefaultRunContext_ctxCancelKillsProcess` — ctx cancel killed only the direct child, and dash (unlike bash) forks `sh -c`, so the surviving grandchild held the output pipes and `Wait` hung 30s — `runContextEnv` now sets `Setpgid` and kills the process group on cancel; (3) `TestExecRunner_verboseStreamsAndCaptures` (smb, latent in mounts too) — two distinct `io.MultiWriter`s capturing into one `*bytes.Buffer` means two pipes, two concurrent copy goroutines, and a data race that silently lost writes — capture now goes through a shared `executil.LockedWriter` (`TestLockedWriter_concurrentStreamsDeliverAllBytes`), and CI runs `go test -race ./...` to guard the class.
* **Fixed (onboard forced apply, docker-e2e-found)**: real mise 2026.8 refuses `dotfiles apply` over existing files (`refusing to overwrite existing files (use --force)`), failing `dotdrift onboard <live-file>` on all three e2e distros. `mise.Runner.DotfilesApply` gains a `force` param: onboard passes `true` (it just snapshotted the live content into the module — takeover is lossless), the apply pipeline's dotfiles step passes `false` (never clobber files it does not own). New tests: `TestOnboard_dotfilesApplyForced`, `TestExecMise_dotfilesApplyForceFlag`. Verified via `act`: CI test+vet+lint green, docker-e2e PASS on debian/ubuntu/cachyos.
* **Fixed (onboard default mode, docker-e2e-found)**: the kong flag `--mode` defaulted to `symlink-each`, overriding `internal/onboard`'s `symlink` default — real mise rejects `symlink-each` for file sources, so `dotdrift onboard <file>` failed in docker-e2e with `mode symlink-each requires the source to be a directory`. The CLI default is now `symlink`, matching contract.md invariant 5 and the internal default. New test: `TestOnboard_modeFlag_defaultIsSymlink`.
* **Fixed (packages step, remove failure tolerance)**: a failing package removal (e.g. `paru -R` exiting 1 because some packages are already missing) no longer fails the whole apply — the packages step logs a zerolog warning carrying the remove error and proceeds to install. Removal is best-effort cleanup; install failures still fail the step. `TestPackagesStep_removeErrorFails` replaced by `TestPackagesStep_removeErrorWarnsAndProceeds` (asserts nil error, install still called, warning logged); `docs/tasks/t4-packages.md` updated.

## 2026-07-29

* **Added (verbose: `-v` alias + `set -x` command echo)**: `--verbose` on `apply` and `onboard` gains the short alias `-v` (kong `short:"v"`; no collision — no other short flags exist and `version` is a subcommand), and verbose mode now echoes every executed child command line to the Err writer (stderr, like bash `set -x`) immediately before it runs: `+ <argv>` with args shell-joined — plain args unquoted, whitespace- or quote-bearing args single-quoted (`'` escaped `'\''`). One shared helper (`internal/executil`: `ShellJoin` + `EchoCommand`) serves all four streaming call sites — mise `runOp`, packages `RunStream`, mounts and smb `ExecRunner` verbose paths — so there is no per-site duplication. Echo only happens on the streaming/verbose path: captured paths never echo, so probes stay silent AND unechoed (`mise --version`, `pacman -Q`/`dpkg -l`/`rpm -q` IsInstalled checks, mise.run bootstrap), the smb/mounts captured-output contracts stay clean (echo goes only to Err, never into the parse buffer), non-verbose runs remain byte-identical, and stdout stays parseable. Interface shapes unchanged. TDD (RED-proven): `TestShellJoin_*` (plain/spaces/tab/quote-escape/empty) + `TestEchoCommand_setXStyleLine` (executil), `TestExecMise_verboseEchoesCommandLine` (all four ops, echo precedes the command's own stderr) + `TestExecMise_nonVerboseNoEcho` + `TestMise_versionProbeNeverEchoedWhenVerbose` (mise), `TestExecRunner_RunStream_verboseEchoesCommandLine` + `TestExecRunner_Run_neverEchoesEvenWhenVerbose` (packages), `TestExecRunner_verboseEchoesCommandLine` (mounts + smb; smb also asserts the echo never enters the returned capture), `TestKong_verboseShortFlagParses` (cmd). `go test ./...` and `go vet ./...` green.

* **Added (--verbose output streaming)**: `dotdrift apply` and `dotdrift onboard` gain `--verbose` (default false; kong `DefaultEnvars("DD")` makes `DD_VERBOSE=1` work with zero extra code). When set, child-process stdout/stderr streams live to the terminal instead of being captured and discarded: mise operations (`ExecMise.EnsureAndInstall`/`DotfilesApply`/`DotfilesApplySudo`/`RunTask` via the new `Mise.runOp`, streaming failure returns the bare err since the output is already visible), package manager install/remove (`packages.ExecRunner.RunStream`; backends route `Present`/`Absent` through `runMutating`, which picks the streaming surface when the runner offers one), and mounts/smb activation commands. Probes always stay captured: `mise --version` (parsed), `pacman -Q`/`dpkg -l`/`rpm -q` (`IsInstalled` keeps calling `Runner.Run` directly), and the mise.run bootstrap. mounts/smb `ExecRunner`s stream via `io.MultiWriter` so captured-output contracts survive verbatim — mounts errors keep their output suffix, smb's `id -Gn`/`pdbedit -L` parsing and the testparm gate's error output are untouched. No flag → byte-identical behavior. Interface shapes (`mise.Runner`, `packages.Backend`/`Runner`, `mounts.Runner`, `smb.Runner`) unchanged: cmd sets `Verbose` on the concrete `*mise.Mise` and passes the flag through a narrow `interface{ SetVerbose(bool) }` seam (`setVerboseRunner`), so existing fakes work untouched; the mounts/smb construction seams now return `*ExecRunner`. Onboard's real-mise path now goes through the shared `defaultMise` seam. TDD (RED-proven): `TestExecMise_verboseStreamsOperationOutput` (all four ops), `TestExecMise_nonVerboseDiscardsSuccessOutput`/`_nonVerboseErrorAppendsCapturedOutput`/`_verboseErrorReturnsBareErr`, `TestMise_versionProbeStaysCapturedWhenVerbose`, `TestExecRunner_RunStream_verboseStreamsToWriters`/`_nonVerboseBehavesLikeRun`, `TestBackend_presentAbsentUseStreamingPath`/`_isInstalledStaysOnCapturedPath` (Paru/Apt/Dnf tables), `TestBackend_setVerbose`, mounts `TestExecRunner_verboseStreamsAndPreservesErrorOutput`/`_nonVerboseSuccessDiscardsOutput`/`_nonVerboseErrorAppendsOutput`, smb `TestExecRunner_verboseStreamsAndCaptures`/`_nonVerboseCapturesOnly`, `TestExecRunner_setVerbose` (mounts+smb), `TestApply_verbosePropagatesToRunners`/`_nonVerboseLeavesRunnersQuiet`, `TestOnboard_verbosePropagation`, `TestKong_verboseFlagParses`/`_verboseDefaultEnvar`. `go test ./...` and `go vet ./...` green.

* **Added (module filter)**: `dotdrift modules`, `dotdrift plan`, and `dotdrift apply` accept optional positional module ids limiting the command's scope (`dotdrift apply vim git` ≡ `dotdrift apply vim,git`; `profile.ParseModuleFilter` splits on commas, trims whitespace, drops empties, dedupes first-seen). `Profile.LimitTo` validates the ids: unknown ids error naming them plus every valid module id; an id naming an already-skipped module (disabled / `when` filter) errors naming it and its existing skip reason — the filter never resurrects skipped modules. Excluded selected modules move to `Skipped` with reason `module filter`, so `modules` shows them and `plan`/`apply` resolve only the listed scope. The stale "optional `--only` later" note in cli-surface is removed — this feature supersedes it. Fingerprint caveat documented: a filtered apply changes the selection fingerprint, so resume state resets via the existing selection-change path (alternating scoped/unscoped applies resets the cursor each time). TDD: `TestParseModuleFilter`, `TestProfile_LimitTo_*` (keeps-only-listed, order, unknown, disabled, when-filtered, empty no-op), `TestCLI_modules_filter*`, `TestCLI_plan_filterLimitsScope`, `TestApply_moduleFilterLimitsSelection`/`_UnknownErrors`, `TestKong_applyPositionalModules`. `go test ./...` and `go vet ./...` green.

* **Refactored (dotfile mode vocabulary)**: renamed dotdrift's dotfile mode `link` → `symlink` everywhere, making dotdrift's mode vocabulary exactly mise's (`symlink`, `symlink-each`, `copy`, `template`). The `mise.MiseDotfileMode` translation layer (introduced in 05cc0fa when real mise was found to silently ignore `mode = "link"`) is deleted — modes now pass through to the generated `mise.toml` unchanged, matching the directness `symlink-each` already had. This resolves the known gap documented 2026-07-26: onboard's default mode no longer no-ops against real mise because the default *is* a valid mise mode. Onboard's default and the kong enum are now `symlink`; resolve-time mode validation, all module.toml fixtures (testdata, examples, e2e demo), and the product/task docs follow. Tests: `TestGenerateDotfiles_translatesLinkToSymlink` removed, `TestGenerateDotfiles_passthroughModes` now covers all four modes, `TestOnboard_defaultModeLink` renamed to `TestOnboard_defaultModeSymlink`. `go test ./...` and `go vet ./...` green.

* **Fixed (test hermeticity, CI)**: `TestPathMap_homeAndSystem` hardcoded `/etc/pacman.conf` as its "system path", which exists only on Arch/CachyOS — the test failed on GitHub Actions' Ubuntu runners (`stat /etc/pacman.conf: no such file`). The test verifies `mapPath`'s pure string-mapping logic (home→`home/`, absolute→`system/`); the file-existence requirement was incidental to exercising it through `Run`. Now uses a writable temp file the test owns, so it passes on any OS with no CI-platform change.

* **Added (e2e, Arch/CachyOS coverage)**: the Docker e2e suite gains a CachyOS image (`tests/e2e/Dockerfile.cachyos` on `cachyos/cachyos:latest`) alongside the debian-family pair — the first real coverage of dotdrift's Arch backend (`detect.backendForDistro` → `paru`; `paru -S --needed` install, `pacman -Q` idempotency). CachyOS ships `paru` in its own binary repo (`cachyos/paru`), so no AUR build is needed, and paru installs repo packages as root. Added `cachyos` to `run.sh`'s OS loop; the existing `e2e.yml` job drives it unchanged. `scenario.sh` is now package-manager-aware (`have_pacman` branches `pacman -Q` vs `dpkg -l`).

* **Fixed (e2e fixture, Arch portability)**: the shared e2e test package changed from `curl` to `jq`. `curl` is a hard dependency of `pacman` itself on CachyOS (Required By: pacman, git, gettext, libelf, tpm2-tss) and cannot be removed, so the debian/ubuntu "purge curl to force a real install" trick was impossible there. `jq` is a leaf package absent from all three base images, decoupling the install-under-test from `curl` (which the images keep only for the `mise.run` bootstrap); the scenario asserts jq absent before apply and present after, proving a real install on every distro. Verified: `./tests/e2e/run.sh` → PASS debian, ubuntu, cachyos.

* **Added (install.sh + README install)**: standalone `install.sh` at the repo root — resolves the latest release by following the `releases/latest` redirect to its tag (or an optional positional version arg, e.g. `./install.sh v0.4.0`), downloads the `dotdrift_<tag>_linux_<arch>.tar.gz` archive plus `sha256sums.txt`, verifies the SHA-256 checksum (`--ignore-missing` since the checksum file lists every arch), and installs the binary to `$DOTDRIFT_BINDIR` (default `~/.local/bin`). Linux only (`amd64`/`arm64`); requires `curl`, `tar`, `sha256sum`; warns when the bindir isn't on `PATH`. README's Installation now leads with `curl -fsSL .../main/install.sh | bash` (and `| bash -s <version>` / `DOTDRIFT_BINDIR=...` variants) as the recommended path, with `go install` demoted to a secondary "From source" section; the previous inline download snippet was removed. Smoke-tested end-to-end: latest and pinned runs both verified checksums and installed a working binary (`dotdrift v0.4.0_ae1410d`), and the piped `bash -s` form matches the documented README invocation.

* **Changed (install.sh wget fallback)**: `install.sh` now uses `curl` when available and falls back to `wget` otherwise — HTTP fetches route through a new `http_get` helper, and `latest` resolution uses curl's redirect-following, falling back to the GitHub releases API JSON (`tag_name` parsed via `sed`, no `jq` needed). The dep check relaxed from requiring `curl` to `curl` **or** `wget`; `tar`/`sha256sum` still required. Verified all three paths end-to-end: curl (latest + pinned), wget with curl hidden from `PATH` (latest via the API + pinned, checksum verified, working `dotdrift v0.4.0_ae1410d` installed), and neither client present → loud `required command not found: curl (or wget)` error.

## 2026-07-28

* **Implemented (review suggestions S1–S5)**: the adversarial review's five improvement suggestions, all TDD: (S1) CLI⇔TUI equivalence matrix extended to volume-kind mounts (uid/gid token expansion), custom `--option`, `--state disabled`, and smb `--readonly` — parity proven, no drift found; (S2) integration-tagged validators (`internal/generate/verify_integration_test.go`): `systemd-analyze verify` on every golden unit under its proper EscapePath-derived name, the raw-content/escaped-name contract locked with a negative control (escaped `Where=` is rejected by live systemd — escaping content would be a bug), and `testparm -s` on the golden shares.conf; (S3) wizard presentation decisions extracted into pure tested functions (`internal/tui/decisions.go`: kindForDefaults, volumePathAction, prefillForVolume, shareLoopDone, tab-switch param mappings) and `MountsWizard.Write` now rejects zero mounts (CLI/smb parity); (S4) cmd↔tui helper triplication unified into `internal/tui/shared.go` (`InvokingUser`, `ExistingMountSources` with fail-loud decode errors, `PrintSummary` to the caller's writer) — six divergent duplicates deleted; (S5) docs accuracy round: migration recipe's unit-name claim corrected with live-systemd evidence, testparm gate cataloged as a deliberate delta, registry.toml schema comment completed (`absent`, packages-required), contract invariant 14 aligned with the static status denominator, `writer.go` split (200/36/45 pure LOC, all under the ceiling), dead test anchor removed.

* **Closed (oracle nits, generate)**: Oracle's final round verified the M13 work and flagged two documentation drifts, now fixed: `unionAbsent`'s docstring states the actual union-only present-list semantics (a module that ever required a package keeps it installed), the migration recipe's apply section documents `packages.absent = ["ntfs-3g"]` for ntfs3 mounts (with the both-drivers skip), and `t-generate.md`'s registry notes cover the `absent` field and `unionAbsent`. The M13 `generate` milestone is complete: reviewer gate approved after one fix round, Oracle verified after two.

## 2026-07-27

* **Oracle-verification fixes (generate)**: second adversarial pass (Oracle) found five gaps, all fixed TDD: (1) stale-artifact GC now warns via zerolog when a removed unit may still be live, with the exact `systemctl disable --now` + `rm` remediation command (`TestWriter_gcWarnsAboutLiveUnit`) — the placed-unit orphan is surfaced instead of silent; (2) the smb wizard can no longer reach the write confirm with zero shares — declining "add a share?" with none configured prints "at least one share is required" and falls through to the share prompt, matching the CLI's rejection (state-machine guard locked by `TestSmbWizard_writeRequiresShare`); (3) the registry gained `absent` packages per entry — `ntfs3` carries `absent = ["ntfs-3g"]`, mirroring the legacy script's kernel >= 7.1 cleanup, unioned into `packages.absent` by the writer with a self-conflict skip for modules using both drivers (`TestRegistry_ntfs3AbsentCarriesRemoval`, `TestWriter_absentPackagesUnioned`, `TestWriter_absentSkipsSelfConflict`); (4) dead exported `generate.LoadDefault` deleted; (5) migration recipe documents the cosmetic After= blank-line delta. Gates: `go test ./...`, `go vet`, `golangci-lint` (0 issues), `-race` — all green.

* **Reviewer-gate fixes (generate)**: adversarial review found one major defect and minors, all fixed: (1) `Input.Mounts == nil` previously wiped a module's `[mounts]` and GC'd its unit files when running `generate smb` at the same module — nil now means "not managed by this run", mirroring the nil-Smb semantics (regression test `TestWriter_mountsNilLeavesExistingAlone`); (2) mounts/smb now require `scope = "system"` — a user-scope module declaring either fails at resolve naming the module instead of dying inside mise (`TestResolveMounts_userScopeModuleErrors`, `TestResolveSmb_userScopeModuleErrors`; 11 pre-existing merge fixtures gained the explicit scope); (3) profile-layout's `[smb] group` mislabeled "workgroup name" corrected to the Linux share-access group; (4) migration recipe notes the legacy bare-`ntfs` preset gap and the `enable --now` strictness delta (legacy swallowed failures, dotdrift fails loudly); (5) deleted cmd's duplicated `generateModuleDir` in favor of the exported `generate.ModuleDir`. Remaining nits documented without action: zero-value field noise in generated module.toml (BurntSushi has no omitempty), static status denominator 8 (consistent with the conditional-step pattern).

* **Added (generate TUI)**: interactive wizard for `dotdrift generate` in new package `internal/tui` — charmbracelet huh forms + lipgloss tab chrome (mounts | smb | quit switcher). Architecture: a pure spec-builder state machine (`MountsWizard`, `VolumeChoices`, `ValidateNetworkSource`, kernel-`Recommend`ed type defaults, already-managed volume marks, flag pre-fill) plus a thin huh presentation layer, so wizard logic is hermetically testable without a TTY. CLI and wizard share one assembly path (`tui.MountsInput`/`SmbInput`/`ParseShareFlags`/`ResolveWritable`) — CLI⇔TUI byte-identical module trees proven by `TestGenerate_cliTuiEquivalence_{mounts,smb,smbDefaultAvahi}` (S5). Mounts flow: layer/module/target → volume (lsblk MultiSelect, managed pre-marked) or network (regex-validated source) → type select with recommended default → preset options as checkboxes + free-form additions (accepting the preset verbatim records nil so the registry preset applies) → optional startat → state → review → single `WriteModule` per run (multi-mount loop accumulates; wholesale replace means one write). `generate.ModuleDir` exported for the wizard's already-managed detection. Deps: huh v1.0.0, bubbletea v1.3.6, lipgloss v1.1.0 (vendored; offline `GOFLAGS=-mod=vendor go test ./...` green). Contract out-of-scope list no longer excludes interactive TUI.

* **Added (generate CLI)**: `dotdrift generate mounts|smb` (cmd/generate.go + generate_support.go). Flags map 1:1 to spec keys; strict mode selection — input flags → CLI mode (loud required-field validation), zero flags + TTY → wizard seam (`runWizard`/`wizardInvocation`, owned by T13), zero flags + no TTY → actionable error, `--tui` forces wizard (errors without TTY). `--list-volumes` prints the lsblk table with MANAGED marks against the target module's existing mounts. Manual QA: generated an NFS mount at the host layer via `go run . generate mounts ...` — units + module.toml correct, rerun idempotent. cli-surface gained the full generate section; README the command row. Known cosmetic: BurntSushi encodes zero-value fields (`id = ""`, empty `[when]`/`[hooks]`/`[smb]` tables) in generated module.toml — semantically equivalent to absent.

* **Added (module writer)**: `generate.WriteModule(root, Selection{Layer,Hostname,Username,ModuleID}, Input{Mounts, Smb, UID, GID})` — idempotent, layer-aware module materialization: renders unit files (+shares.conf) into the module dir, seeds a minimal functional `smb.conf` once (never clobbers hand edits), materializes module.toml (scope=system, wholesale [mounts], [smb] when provided, package union with registry-required packages + samba/avahi, dotfile entries to `/usr/local/lib/systemd/system/` and `/etc/samba/...`), preserving unrelated sections. Stale-artifact GC removes previously generated unit files (tracked + unit-named only) when mounts shrink; byte-identical rerun proven by SHA-256 manifest. smb-nil means "not managed by this run". Unknown mount type errors before any write.
* **Wired (apply/plan/status)**: pipeline is now `hooks-pre → packages → tools → dotfiles → dotfiles-system → mounts → smb → hooks-post` (conditional mounts/smb steps, `newMountsRunner`/`newSmbRunner` seams); status denominator 8 (conditional-step caveat documented). `plan` text gains `mounts:`/`smb:` sections (omitted when empty — no-mounts profiles render byte-identical); `plan --json` gains `mounts`/`smb`. Contract invariant 14 (placement vs activation separation) added; cli-surface updated. New fixture `testdata/profiles/mounts`.

* **Added (renderers)**: `internal/generate/render.go` — pure renderers for `.mount` (After= only for network presets, uid/gid token expansion, options override vs preset), `.service` (oneshot systemctl wrapper), `.timer` (only with startat; Description references the real same-stem service unit — the reference template's `${NAME}.service` was accurate only by naming coincidence), and `shares.conf` (sorted stanzas, comment fallback to share name, `valid users` fallback `@<group>`). Golden files byte-checked against `envsubst`'ed reference templates; unit names via `EscapePath`.
* **Added (mounts step)**: `internal/mounts` — `mounts` apply step (activation only, never writes files): `mkdir -p` all destinations, one `daemon-reload`, then per entry `enable --now <unit>.mount` (+`.timer` with startat) or tolerant `disable --now` for `state = "disabled"` (never fails the step — resume-safe). Sudo argv mirrors the mise pattern (pure `systemctlArgv`/`mkdirArgv` + `geteuid` seam; root direct, non-root `sudo -E`). Recording-fake tests assert exact command order, ctx propagation, escaped unit names.
* **Added (smb step)**: `internal/smb` — `smb` apply step: per-module `getent group`/`groupadd` (default group `smb`), `id -Gn`/`usermod -aG` membership, `avahi-daemon` when avahi unset-or-true, `testparm -s` validation gate (failure stops before any restart, output carried in the error), `is-enabled`/`enable` + `is-active`/`restart smb`, then per declared user `pdbedit -L` check → interactive `sudo smbpasswd -a <user>` on a TTY, warn-and-skip with the manual command on the Out writer otherwise. Stdlib-only TTY probe (char-device check; x/term not vendored).

* **ADR-0002 implemented (spec + merge)**: `module.toml` gains `[mounts.<name>]` (source/destination/type/options/startat/state) and `[smb]` (group/users/avahi + `[smb.shares.<name>]` path/comment/valid_users/writable/public) — new `internal/profile/spec.go`. Resolve merges whole-entry by name across layers (dotfile winner-map semantics); smb scalars replace when set (`users` replaces, deliberately unlike hooks' append; `avahi` is `*bool` so unset≠false, downstream default true). `resolve.Plan` gains `Mounts`/`Smb` aggregates carrying name/spec/module/layer/scope, sorted deterministically. Structural validation at resolve names module + entry + field; mount `type` is never registry-validated (registry independence). profile-layout/merge-rules/contract docs updated; 13 new tests incl. a no-mounts/no-smb regression guard on the existing fixture.

* **ADR-0001 completed (resolve)**: resolve now derives layer paths from `p.Root` + the module directory name (`rootFromModule` deleted) so overlay-only modules merge against an absent/empty base layer; malformed overlay `module.toml` errors still propagate naming module id + overlay path; scope documented as representative-layer only (base preferred, overlay `scope` ignored). Tests in `internal/resolve/overlay_test.go` (3 RED-proven: host-layer winner label, discovery-layer scope, directory-name overlay keying across host→user; 2 regression locks). merge-rules.md gained the scope rule and an overlay-only section.
* **Added (lsblk detection)**: `generate.Volumes(ctx, reg, sources)` — runs `lsblk -J` through the unexported `lsblkRun` seam, walks the device tree, filters to partitions with a UUID whose fstype the registry classifies as `volume` (direct type or family match, so lsblk `ntfs` matches the ntfs family), marks `Managed` for UUIDs already present as `UUID=` mount sources, sorted by UUID. Fixtures: recorded real `lsblk -J` from the dev machine + a synthetic tree (nested crypt child, rom/loop, empty UUID, null fstype, zfs, nfs). TDD caught a real bug: null fstype unmarshals to `""` and spuriously matched family-less entries — fixed with an explicit empty-fstype guard.

* **Designed (generate command)**: Grilled design for `dotdrift generate {mounts,smb}` recorded in `CONTEXT.md` (glossary: layer vs scope, placement vs activation, mount, share) and ADRs 0001 (overlay-only module discovery) and 0002 (mount/smb specs in module.toml, whole-entry merge by name).
* **ADR-0001 implemented (profile)**: `discover()` now scans `hosts/<hostname>/modules/` and `users/<username>/modules/` in addition to `modules/` — presence = managed holds per layer. Layers dedup by directory name (base → host → user representative precedence); declared-id collisions across different directory names stay fatal; empty hostname/username facts skip that layer's scan. This repairs the latent `onboard --host` dead path (host-layer modules it wrote were never selected). New fixture `testdata/profiles/overlayonly`; tests `TestDiscover_hostOnlyModuleSelected`, `TestDiscover_userOnlyModuleSelected`, `TestDiscover_conflictingIDsAcrossLayersStillFatal` + guard tests. `discover`/`loadModule` split into `discover.go` (profile.go was over the 250 pure-LOC ceiling pre-change). Contract invariant 1 and profile-layout updated.
* **Added (generate registry)**: new package `internal/generate` with an embedded TOML filesystem registry (`registry.toml`: default, btrfs, exfat, vfat, ntfs3, ntfs-3g, nfs, cifs — presets verbatim from the pimp-my-cachyos reference script, `uid`/`gid` as bare substitution tokens) plus whole-entry override merge from `$XDG_CONFIG_HOME/dotdrift/generate.toml` (env `DOTDRIFT_GENERATE_REGISTRY` overrides the path; missing file is silent, malformed names the path). `recommended_if` evaluator supports only `kernel <op> <version>` with zero-padded numeric segment compare (`7.10 > 7.1`); kernel release behind the `KernelRelease` seam. Tests: embedded-loads schema, golden preset match vs the reference script, override merge/add, kernel-op table, injected seam ntfs3-vs-ntfs-3g selection, malformed expression/TOML errors.
* **Added (systemd-escape port)**: `generate.EscapePath` — a pure-Go port of `systemd-escape --path` validated against 31 ground-truth vectors captured from the real binary (systemd 258): duplicate `/` collapse, literal `-` escaped as `\x2d`, leading `.` escaped, UTF-8 byte-wise escaping, empty result → `-`. Documented divergence: the real binary hard-fails on non-normalized mid-path `..`; `EscapePath` normalizes. Live-binary cross-check test skips cleanly when `systemd-escape` is absent.

## 2026-07-14

* **Creation**: Initial OKF plan for dotdrift (TDD milestones, mise ensure, defaults-first CLI).
* **Update**: Renamed project forge → dotdrift; apply always resumes; onboard enable-by-presence.
* **M0 complete**: CLI skeleton with all subcommand stubs, `--help` works without `os.Exit` in tests, `go test ./...` green.
* **M1 complete**: Implemented `internal/facts`, `internal/profile`, and `dotdrift modules`. Added fixture profiles under `testdata/profiles` and TOML-based selection with disable-union and `when` filters.
* **M2 complete**: Implemented `internal/resolve`, `dotdrift plan`, host/user overlays, and selection fingerprint.
* **M3 complete**: Implemented `internal/state` and `internal/apply` resume-only pipeline driver.
* **M4 complete**: Implemented `internal/packages` paru/pacman backend.
* **M5/M6 complete**: Implemented `internal/mise` bootstrap, tools/dotfiles steps, and mise config generation.
* **M7 complete**: Implemented `internal/detect` for host, user, os, gpu, and backend facts.
* **M8 complete**: Implemented `internal/onboard` and `dotdrift onboard`.
* **M9 complete**: Hardened `cmd/init` and `cmd/status` with tests, created `README.md` and `examples/profile`, and updated `docs/log.md`. Final `go test ./...` green.
* **M10 complete**: Added `Apt` and `Dnf` backend skeletons with tests, `auto` backend resolution from os-release, and advisory `flock` around state file load/save.
* **Resumed**: Fixed `cmd/root.go` help handling to print `--help` in the real binary while keeping tests `os.Exit`-safe; `main.go` now prints `ExitError` details; cleaned duplicate `/etc/os-release` read in `internal/detect`; implemented `internal/mise` system-wide vs user-managed classification per OKF with `DOTDRIFT_MISE_SYSTEM` override and self-update preference; closed remaining gaps against docs: `init` now creates `modules/`, `hosts/`, `users/`; `onboard` accepts `--profile`; mise bootstrap also checks well-known paths `~/.local/bin/mise` and `~/.local/share/mise/bin/mise`; fixed `.golangci.yml` for v2 schema (`linters.settings` instead of `linters-settings`) and made the project lint-clean.

## 2026-07-15

* **Planned**: Make default state file profile-specific under the XDG state directory so resume state does not collide across profiles; wire `packages.absent` through to the backend `Absent` method so explicit exceptions actually uninstall packages.

* **Implemented**: Default state path is now profile-specific under the XDG state directory (`$XDG_STATE_HOME/dotdrift/profiles/<hash>/state.json`, defaulting to `~/.local/state/dotdrift/profiles/<hash>/state.json`) for `apply` and `status`; `packages.absent` is now propagated through `resolve.PackagesStep.Remove` and executed by the package backend during apply. Verified with `go test ./...`, `go vet ./...`, `golangci-lint run ./...`, and `go build`.

* **Updated**: `docs/tasks/t3-state.md` and `docs/tasks/t4-packages.md` now describe the XDG profile-specific state path and the `packages.absent` uninstall behavior. Added `TestDefaultPath_*`, `TestProfileStatePath_defaultsToLocalState`, and `TestPackagesStep_noPackagesNoBackendCalls`.

## 2026-07-19

* **M12 complete (scope)**: `module.toml` gains top-level `scope = "user" | "system"` (default `user`; `profile.ScopeOrDefault`), validated per selected module at resolve time (`module %s: unknown scope %q (valid: user, system)` — same fail-loud pattern as dotfile modes, fires even for dotfile-less modules). `resolve.DotfileEntry` carries its module's scope (base layer only, like `id`/`app`). One `dotdrift apply` now covers both scopes: `cmd/apply.go` partitions entries, applies user entries through the existing `DotfilesStep` against a scope-filtered plan copy, and appends a conditional `dotfiles-system` step (`mise.DotfilesSystemStep`, config at `<state>/mise/dotfiles-system/mise.toml`) after `dotfiles` when system entries exist — the D8a pre-pipeline full config still contains everything. System entries apply via the additive `ExecMise.DotfilesApplySudo`: `sudo -E <mise> dotfiles apply --cd <dir> [--yes]` when non-root (the `-E` preserves the `MISE_TRUSTED_CONFIG_PATHS` trust env through the elevation), direct when EUID 0 (containers); pure decision function `dotfilesApplyArgv` with an injectable `geteuid` seam. `Runner` interface, `FakeRunner`, and `internal/onboard` untouched. `pipelineStepNames` gains `dotfiles-system` (conditional-step caveat documented; status denominator is 6). Plan: text marks `module: <id> [system]`, `plan --json` carries `scope` per dotfile entry; `dotdrift modules` unchanged. Packages self-elevate and hooks keep inline privilege — no scope machinery for either (contract invariant 13). New fixture `testdata/profiles/scope`. Tests: `TestModuleScope_*`, `TestResolveScope_*`, `TestGenerateDotfiles_systemEntriesRoundTrip`, `TestDotfilesApplyArgv_*`, `TestExecMise_dotfilesApplySudo_*`, `TestDotfilesSystemStep_*`, `TestApply_dotfilesSystemStep`, `TestApply_noSystemEntriesSkipsDotfilesSystem`, `TestCLI_plan_scopeMarker`, `TestCLI_plan_jsonScope`, updated `TestStatus_*`. Docs: contract invariant 13, profile-layout `scope`, cli-surface sudo/plan notes, README scope section, new `docs/milestones/m12-scope.md` + `docs/tasks/t-scope.md`, indexes. `go test ./...`, `go vet ./...`, `golangci-lint run ./...` green.

* **Added (onboard)**: `--force` flag (`onboard.Options.Force`) re-materializes a conflicting path: instead of the `conflict: ... already exists in module` error, the existing module destination is removed (`os.RemoveAll`, files and dirs alike) and re-copied fresh from the live path. Default behavior unchanged. New tests: `TestOnboard_forceReplacesExistingFile`, `TestOnboard_forceReplacesExistingDir`, `TestOnboard_forceFlagParses`, `TestOnboard_forceFlowsToOptions`. Docs: cli-surface onboard flags table, README command table, t8-onboard.

* **Added (cli)**: `dotdrift plan --json` prints the effective plan as a single JSON object to stdout (`fingerprint`, `modules`, `packages.install`/`remove`, `tools`, `dotfiles[]` with target/source/mode/module/layer). The `warning: no modules selected` line is suppressed in JSON mode so stdout stays parseable; default text output is byte-identical. New tests: `TestCLI_plan_jsonOutput`, `TestCLI_plan_jsonNotInDefaultOutput`, `TestCLI_plan_jsonNoModulesStaysParseable`.

* **Removed (dead exports + hooks noop, decision D4a)**: deleted `facts.Detect` (package kept — `facts.Facts` is the shared facts type used by profile/resolve/detect/cmd), package-level `state.Load`/`state.Save` (FileStore is the API), `detect.DetectEnv`, `apply.Pipeline.StepNames`/`Steps` and `apply.NoOpStep`/`NewNoOpStep`, the unused `mise.DotfileEntry`/`mise.Plan` mirror types, and the permanent-noop hooks step: `mise.HooksStep`, `resolve.Plan.Hooks`/`resolve.HooksStep`, the `hooks:` section of `dotdrift plan` output, and the `&mise.HooksStep{}` pipeline entry in `cmd/apply.go`. Pipeline is now `packages → tools → dotfiles`; task docs t2/t3/t6 updated.

* **Hardened (root)**: pprof now defaults to `127.0.0.1:6060` (was `0.0.0.0:6060`) and its `ListenAndServe` error is logged via zerolog `Error` instead of silently swallowed; `.env` auto-load honors `DOTDRIFT_NO_ENV=1` (skips loading entirely) and emits a `Debug` notice naming the loaded files. New tests: `TestProfilingListenOn_defaultIsLoopback`, `TestLoadDotenvFiles_loadsEnvFiles`, `TestLoadDotenvFiles_optOut`.

* **Added**: `.github/workflows/ci.yml` — CI workflow running `go test ./...` and `go vet ./...` (with `GOFLAGS=-mod=vendor`) plus golangci-lint v2 on push to `main` and pull requests, closing the M0 "CI runs test + vet" exit criterion.

* **Fixed (onboard, M8 re-validated)**: generated mise config moved out of the profile into the XDG state dir (`profiles/<hash>/onboard/mise.toml`) with absolute dotfile sources — previously mise resolved relative sources against `<module>/.mise/` and failed end-to-end; `--yes` plumbed through `cmd/onboard.go` → `onboard.Options` → `mise.DotfilesApply`; directory-tree materialization now preserves per-file modes (ownership not preserved, documented); `TestOnboard_orderCopyThenEnsureThenMiseApply` now records and asserts the copy → ensure → apply sequence.

* **Fixed (state)**: concurrent `dotdrift apply` now serializes on a sidecar lock `<state-path>.lock` (`flock(LOCK_EX)`) held across the entire load→pipeline→save window — the previous per-op flock was orphaned by tmp+rename (live-reproduced lost-update). Corrupt `state.json` errors now carry a recovery hint; `ProfileStatePath` canonicalizes via `EvalSymlinks`; `Save` fsyncs before rename; `DefaultPath` returns an explicit error when no state dir is determinable. Contract invariant 11 added. Race-clean under `go test -race`.

* **Fixed (resolve)**: dotfile `source` paths escaping the layer root (e.g. `../../outside`) are now a resolve-time error naming module and source (contract invariant 8); declared sources missing from all layers are errors; malformed host/user overlay `module.toml` errors are propagated instead of silently ignored; cross-module `present`+`absent` package conflicts are errors (invariant 9); empty hostname/username facts error when modules are selected. Fingerprint scope documented as broader than selection (invariant 10).

* **Fixed (detect)**: username now comes from `os/user.Current()` with `$USER`/`$USERNAME` as fallback and an explicit error when both fail — `sudo dotdrift apply` previously selected the invoking user's overlays while running as root. `merge-rules.md` documents OS-account username, case-sensitive `when` matching, and GPU first-match semantics. README carries a sudo warning.

* **Fixed (packages)**: unknown or unresolvable package backends now fail loudly (`no supported package backend for distro "X"`) instead of silently no-op'ing; `Runner`/`Backend` take `context.Context` and `ExecRunner` uses `exec.CommandContext` so Ctrl-C kills in-flight paru/apt-get/dnf processes; `Apt`/`Dnf.IsInstalled` propagate non-exit-1 errors like Paru; added `dnf_test.go`.

* **Fixed (profile)**: `Load` errors on a missing `modules/` directory (`not a dotdrift profile`) instead of silently loading an empty profile; duplicate module IDs fail naming both paths; an empty hostname/username errors when a `dotdrift.toml` exists at the collapsed overlay path; `dotdrift plan` prints `warning: no modules selected` when selection is empty.

* **Fixed (init)**: `git clone` runs with `Dir` set to the target's parent (no longer dependent on process CWD); clone dir name strips the `.git` suffix; cloned repos must contain `dotdrift.toml` or init errors; newly created profiles are `git init`'d (warn, not fail, when git is unavailable).

* **Docs (cli-surface)**: `--packages`/`--tools` (was `--package`/`--tool`), bare `dotdrift` prints help (was ambiguous "default action"), `detect` fact list includes `distro`, `plan` documented as never touching mise, `init` section describes clone validation and git initialization.

* **Fixed (mise)**: version parsing no longer accepts pre-releases as stable — `2025.1.0-rc1`/`-dev.1`/`+build.5` compare below the release, garbage output is an explicit parse error, and `mise --version` output is scanned for the first leading-digit token. Failing mise subprocesses now return their captured output in the error, and a failed `https://mise.run` install hints at network connectivity or pre-seeding `~/.local/bin/mise`. `Ensure` is memoized per `Mise` (thread-safe `sync.Once`) so an apply run probes mise once instead of three times; bootstrap messages ("mise not found; installing via https://mise.run …", user-upgrade, system-too-old) are emitted via zerolog. `Runner`/`ExecMise` take `context.Context` (`exec.CommandContext`; `EnsureContext`, legacy `Ensure` keeps `Background`) threaded through `ToolsStep`/`DotfilesStep` and `onboard.Options.Ctx`, and generated `mise.toml` escapes `"` and `\` in values.

* **Tested (apply wiring + reset semantics)**: `cmd/apply.go` is now covered end-to-end via package-level seams (`detectFacts`, `profileLoad`, `resolvePlan`, `defaultMise`, `packagesFor`, same pattern as `runGit`): happy-path order (load → resolve → ensure → packages/tools/dotfiles → complete), selection-fingerprint change resetting resume state (with a new zerolog `Warn` when a persisted selection differs), and the D8a crash snapshot — on first-step failure the on-disk state records `current`/`failed` and the pre-pipeline full mise config (both `[tools]` and `[dotfiles]`) survives on disk (now documented in code as intentional crash-recovery semantics). `cmd/onboard.go` gained a thin mapping test (detect hostname → host overlay, Mode/Packages/Tools/Yes → `onboard.Options`) via an injectable `Mise` runner field. `go test ./...`, `go vet ./...`, and `go test -race ./cmd/` all green.

* **UX (cli)**: `dotdrift status` now reports pipeline progress (`progress: N/M steps` counted against the shared `pipelineStepNames` list in `cmd/apply.go` — the single edit point when steps change), the state file last-modified time (`updated: <RFC3339>`), and a resume hint (`next: dotdrift apply  (resumes at <step>)`) when failed or in-progress. `dotdrift apply` prints the effective resolved plan before the pipeline runs, reusing `printPlan` from `cmd/plan.go` via a new `Out io.Writer` field (kong:"-", defaults to stdout, same pattern as `PlanCmd`). The onboard conflict error now suggests remediation: `conflict: %s already exists in module (remove it or onboard into a different module id)`.

* **Tested (coverage backfill, wi-14)**: `defaultLookPath` well-known-paths fallback (`~/.local/bin/mise`, `~/.local/share/mise/bin/mise`, PATH-wins, not-found, non-mise passthrough) via HOME/PATH env seams; `DOTDRIFT_MISE_SYSTEM` variants (only literal `1` forces system-wide; `0`/`true`/`yes`/unset leave a user-managed path user-managed); `onboard --mode` accepts all documented modes — the kong enum gained `symlink-each` (one-line fix, was rejected despite `docs/product/profile-layout.md` and `internal/resolve` accepting it) — and `template`/`symlink-each` land in `module.toml` dotfile entries; inverse-merge direction pinned (base `absent` + host `present` → package installed); `examples/simple` and `examples/profile` load and select under facts `myhost`/`cri`; apply pipeline save-on-failure persists `status=failed`/`current`/`error` to disk via a real `state.FileStore`; `TestStepOrder` renamed to `TestStepOrder_packagesToolsDotfiles` to match the 3-step pipeline.

* **Milestone (gap-remediation program complete)**: all 15 work items from the 2026-07-19 adversarial gap-analysis plan landed — WI-1 CI gate; WI-2 onboard E2E rebuild; WI-3 state locking + UX; WI-4 resolve hardening; WI-5 detect user trust; WI-6 packages honesty + ctx; WI-7 profile validation; WI-8 init git fixes; WI-9 CLI docs; WI-10 cmd/apply integration tests; WI-11 mise hardening; WI-12 dead-surface removal + root hardening; WI-13 UX polish; WI-14 coverage backfill; WI-15 doc-drift corrections. Final gate: `go test ./...`, `go vet ./...`, `golangci-lint run` — all green (0 issues).

* **M11 complete (hooks)**: `[hooks]` `pre`/`post` arrays in `module.toml` (parsed via `profile.ModuleConfig.Hooks`), merged by append across layers (base → host → user) and aggregated across modules in selection order into the restored `resolve.Plan.Hooks` (`resolve.HooksStep{Pre, Post []string}`). Apply pipeline is now `hooks-pre → packages → tools → dotfiles → hooks-post`: hooks execute as mise tasks (`mise run --cd <dir> hooks:pre|hooks:post`) generated into the apply-time `mise.toml` via `mise.GenerateApplyConfig` (`run` command list, `dir` = absolute profile root, env `DOTDRIFT_PROFILE`/`DOTDRIFT_HOSTNAME`/`DOTDRIFT_USERNAME`/`DOTDRIFT_OS`/`DOTDRIFT_BACKEND`); `ExecMise.RunTask` added without changing the `Runner` interface or `internal/onboard`. `mise.HooksStep` skips empty command lists at construction in `cmd/apply.go` and no-ops in `Run` as a second line of defense; hook failure fails its step (resume re-runs it — post-hooks must be idempotent, contract invariant 12). Opt-outs: `dotdrift apply --no-hooks` and `DOTDRIFT_NO_HOOKS=1`. `dotdrift plan` (text and `--json`) renders the actual hook commands; `status` progress denominator is 5. New docs: `docs/milestones/m11-hooks.md`, `docs/tasks/t-hooks.md`; updated contract/profile-layout/cli-surface/README/indexes. Tests: `TestMergeHooks_*`, `TestGenerateHookTasks_*`, `TestGenerateApplyConfig_*`, `TestHooksStep_*`, `TestApply_noHooksFlag`, `TestApply_noHooksEnv`, `TestCLI_plan_hooksSection` plus updated apply/status/plan expectations. `go test ./...`, `go vet ./...`, `golangci-lint run ./...` green.

* **Fixed (mise trust, dogfood-found)**: real runs of `dotdrift onboard --yes` and `dotdrift apply` failed with `mise ERROR … Config files in … are not trusted` — dotdrift generates `mise.toml` under the XDG state dir (`profiles/<hash>/…`), outside mise's trusted paths, and never established trust (every fake-based test passed; every real run failed). `ExecMise.EnsureAndInstall`/`DotfilesApply`/`RunTask` now pass `MISE_TRUSTED_CONFIG_PATHS` covering the config's directory, merged colon-separated with any pre-existing user value (never clobbered). Env threads through a new additive `Mise.Env` field and an unexported per-call `runWithEnv` (no `os.Setenv`, no shared-state mutation, race-free); `DefaultMise` leaves `Run`/`RunContext` nil so the real exec path is env-aware. `Runner` interface and `internal/onboard` untouched. Tests: `TestExecMise_trustsGeneratedConfigDir` (table over all three entry points, white-box fake `mise` script capturing its env), `TestExecMise_trustMergesExistingTrustedPaths`, `TestMise_envAppendedOnDefaultExecPath`. Docs: `mise-bootstrap.md` trust section.

* **Fixed (mise dotfile mode, E2E-found)**: Docker E2E against real mise 2026.7.x exposed that dotdrift's default dotfile mode `link` is not valid mise vocabulary — every generated `mode = "link"` entry was silently ignored by `mise dotfiles apply` ("unknown mode 'link', ignoring entry", exit 0, no symlink created), so the default dotfiles path no-op'd end-to-end while all fake-based tests passed. dotdrift's user-facing vocabulary stays `link` (module.toml, onboard default, docs); `mise.MiseDotfileMode` now translates `link` → `symlink` at the config-generation boundary, with `copy`/`template`/`symlink-each` passing through unchanged (verified empirically against mise 2026.7.10: `symlink`, `copy`, `template` accepted; `symlink-each` accepted, requires a directory source; `link`, `symlink_each`, and empty mode all silently ignored). Dotfile modes are also validated at resolve time against the documented vocabulary (`link`, `symlink-each`, `copy`, `template`) — unknown or omitted modes error naming module and mode instead of no-op'ing inside mise. Tests: `TestGenerateDotfiles_translatesLinkToSymlink`, `TestGenerateDotfiles_passthroughModes`, `TestResolveDotfileMode_unknownModeErrors`, `TestResolveDotfileMode_emptyModeErrors`, `TestResolveDotfileMode_documentedModesPass`. Docs: `profile-layout.md` mode section, contract invariant 3.
* **Added (e2e, first real integration coverage)**: `tests/e2e/` — a Docker-based debian-family suite (`Dockerfile.debian` on `debian:bookworm-slim`, `Dockerfile.ubuntu` on `ubuntu:24.04`, multi-stage `golang:1.26-bookworm` builder with `go build -mod=vendor`). `scenario.sh` runs inside each container: real `mise.run` bootstrap during `dotdrift onboard` of a live file, `plan` content assertions, a real `apt-get install curl` (the scenario purges the image's curl — needed only for the bootstrap — so the packages step must actually install against the intentionally emptied apt index), then asserts `dpkg -l curl`, the `~/.demorc` symlink resolving into the profile module, pre/post hooks in `/tmp/hooks.log`, `"status": "complete"` in the XDG state file, a no-op second apply, and zero `.mise` runtime pollution under `/profile/modules`. `run.sh` builds/runs both containers and exits non-zero on any failure; `.github/workflows/e2e.yml` runs it on push to `main` only. Offline `go test ./...` remains the definition-of-done gate; the suite is additive.

* **Fixed (apt, found by e2e)**: `Apt.Present` now runs `apt-get update` before `apt-get install -y` — in a fresh container the index is empty/stale and install failed with `E: Unable to locate package` (exit 100), live-reproduced by the debian e2e container before the fix. `Absent` is unchanged, and dnf intentionally has no index-update step (it refreshes metadata as part of install — asymmetry noted in code and `docs/tasks/t4-packages.md`). Tests: `TestApt_Present` expects update-then-install argv; `TestApt_Present_updateErrorStopsInstall` locks the short-circuit.

* **Fixed (apply config clobbering, found by e2e)**: the tools/dotfiles steps rewrite their config file with a single section each, which deleted the `[tasks]` hook definitions from the shared apply `mise.toml` and made `hooks-post` fail with `mise: no tasks defined` whenever dotfiles were present. `cmd/apply.go` now points `ToolsStep`/`DotfilesStep` at their own configs (`<state>/mise/tools/mise.toml`, `<state>/mise/dotfiles/mise.toml` — own directories so `mise --cd` still discovers them), keeping the full shared config intact for the hooks steps and strengthening the D8a crash snapshot. Test: `TestApply_stepsDoNotClobberSharedMiseConfig`.

* **Known gaps (documented, out of scope for the e2e work)**: real mise requires configs to be trusted — on a fresh machine dotdrift's generated configs under the XDG state dir are refused until trusted (the e2e scenario sets `MISE_TRUSTED_CONFIG_PATHS=/`, the standard CI knob; dotdrift should arguably trust its own generated configs). Current mise (2026.7) accepts dotfile modes `symlink`/`copy`/`template` and silently ignores dotdrift's default `link` with only a warning — the e2e fixture uses `symlink`; onboard's default `link` mode currently no-ops against real mise.

* **Dogfooded (CachyOS, sandboxed HOME)**: first real end-to-end run of init → onboard → plan → apply → status with real mise 2026.7.10. It immediately exposed three bugs the fake-based suite was structurally blind to: (1) mise trust — mise rejected the XDG-state generated configs (fixed in c983190); (2) mode vocabulary — mise ignores dotdrift's default `link` mode (fixed in 05cc0fa via `link`→`symlink` translation + resolve-time mode validation); (3) hooks-post lost its `[tasks]` when tools/dotfiles rewrote the shared config (fixed in e803868 with per-step config paths). After all three fixes: onboard produces a real symlink into the module, plan renders dotfiles+hooks, apply runs hooks via real mise tasks (markers verified), state reaches `complete` (5/5), second apply re-runs idempotently, `status` shows progress + timestamp. The Docker debian-family suite (tests/e2e) passes on debian+ubuntu with no environment workarounds — it now verifies the trust fix and link mapping directly.
