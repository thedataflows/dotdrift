name: E2E

on:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  docker-e2e:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Run docker e2e suite (debian, ubuntu, cachyos)
        run: ./tests/e2e/run.sh
